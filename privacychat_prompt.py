# -*- coding: utf-8 -*-
"""PrivacyChat_Prompt.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1J_t22KiaeVJyIgqAjchUL26rzlbSQ-zf
"""

import openai
import os
openai.api_key  = 'YOUR KEY'

import ast

## Reading excel file
import pandas as pd
validation_file = pd.read_excel('test_data.xlsx')
validation_file.columns
policies = validation_file['Text'].tolist()

def get_completion(prompt, model="gpt-3.5-turbo-16k"):
    messages = [{"role": "user", "content": prompt}]
    response = openai.ChatCompletion.create(
        model=model,
        messages=messages,
        temperature=0, # this is the degree of randomness of the model's output
    )
    return response.choices[0].message["content"]

extraction = []
for i in range(0,len(policies)):
  text = policies[i]
  prompt = f"""
Your task is to answer the following questions from the text delimited by triple backticks and output each answer to the question as an element of a list. The final output must be a list with 10 answers.\
Questions:
1 - Does the text talks about information/data collection? Answer only in yes or no.\
2 - Does firm collects user data? Answer this question if the answer to question 1 was 'Yes' and answer only in yes or no\
3 - List exactly what user information is being collected if not specified answer 'None'? Answer this question if the answer to question 2 was 'Yes'\
4 - Does the text talks about information/data sharing? Answer only in yes or no.\
5 - Does firm shares user data? Answer this question if the answer to question 4 was 'Yes' and answer only in yes or no\
6 - List exactly with whom user information is shared? If not specified answer 'None'. Answer this question if the answer to question 5 was 'Yes'\
7 - Does the text talks about for how long user information/data is stored? Answer only in yes or no.\
8 - List exactly how long the user information is stored? If not specified answer 'None'. Answer this question if the answer to question 7 was 'Yes'\
9 - Does the text talks about any rights that user may have associated with their personal information/data? Answer only in yes or no.\
10 - List exactly what rights user have on their personal data? If not specified answer 'None'. Answer this question if the answer to question 9 was 'Yes'\
```{text}```
"""
  response = get_completion(prompt)
  print(response)
  response_list = ast.literal_eval(str(response))
  extraction.append(response_list)

Q1 = []
Q2 = []
Q3 = []
Q4 = []
for i in range(0,len(extraction)):
  Q1.append(extraction[i][2])
  Q2.append(extraction[i][5])
  Q3.append(extraction[i][7])
  Q4.append(extraction[i][9])

df_output = pd.DataFrame({"Q1":Q1, "Q2":Q2, "Q3":Q3, "Q4":Q4})
df_output.to_excel("predictions_iconf.xlsx")